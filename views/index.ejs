<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather App</title>
    <link rel="stylesheet" type="text/css" href="/style.css">
    <script src="https://kit.fontawesome.com/227913b9a4.js" crossorigin="anonymous"></script>
</head>

<body>
    <div class="container">
        <h1>Weather app using NodeJs</h1>
        <form action="/weather" method="get">
            <input type="text" name="city" placeholder="Enter the city name" />
            <button type="submit">Get Weather Details</button>
        </form>

        <% if (error) { %>
            <p class="error">
                <%= error %>
            </p>
            <% } else if (weather) { %>
                <div class="card">
                    <!-- Add the weather logic-->
                    <p>
                        <i class="fa-solid fa-temperature-half"></i>
                        <%= weather.main.temp %>&deg;F
                    </p>

                    <p>
                        <i class="fa-solid fa-cloud"></i>
                        <%= weather.weather[0].main %>
                    </p>

                    <p>
                        <i class="fa-solid fa-location-dot"></i>
                        <%= weather.name %>, <%= weather.sys.country %>
                    </p>
                </div>
                <% } %>
    </div>

</body>

</html>